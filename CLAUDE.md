# Dashboard (Vite + TypeScript)

News-Aggregator PWA. Vanilla TypeScript, kein Framework.

## Commands

- `npm run dev` - Vite Dev Server
- `npm run build` - Production Build (tsc && vite build)

## Status

- Funktioniert, dist/ existiert, deploybar
- Uncommitted: voice.ts, style.css, main.ts, i18n.ts
- Voice Studio: UI existiert, Backend auf Port 3457 fehlt
- Git: 2 Commits auf master, remote up to date (ohne uncommitted)
